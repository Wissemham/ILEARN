{% extends "base.html.twig" %}
{% block body %}
<style>
table, tr, td ,th {
  border: 3px solid black;
  padding-left: 20px;  
}
table{
    width:100%;
    height:30%;
}
tr{
  height:30%;
}
button{
  padding-left:20px;
  padding-top:120px;
  padding-right:20px;
}
</style>

<table>
<tr>

        <th>num question</th>
        <th>contenu</th>
        <th>reponse</th>
        <th>point</th>
        <th>Modifier</th>
        <th>Supprimer</th>
        <th>reponses</th>
</tr>

{% for i in cc %}
<tr>

                <td>{{i.numquestion}}</td>
                <td>{{i.contenu}}</td>
                <td>{{i.reponse}}</td>
                <td>{{i.point}}</td>
                <td> <a href="{{path('questionUpdate',{id:i.iddevoir.iddevoir})}}"> Modifier  </td>
                <td> <a href="{{path('questionremove',{id:i.iddevoir.iddevoir})}}"> Remove  </td>
                <td> <a href="{{path('app_reponse',{id:i.idquestion})}}"> reponse </td>

</tr>

{% endfor %}
</table>

<button type="button"> <a href="{{path('ajoutquestion',{id:idd})}}"> Ajoute </button>

{% endblock %}

