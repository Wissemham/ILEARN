{% extends "base.html.twig" %}
{% block body %}
<style>

table, tr, td ,th {
  border: 3px solid black;

}
table{
    width:100%;
    height:30%;
}
tr{
  height:30%;
}
button{
  padding-left:20px;
  padding-top:120px;
  padding-right:20px;
}
</style>
<div class="container" >
<table class="top">
<tr>
        <th>nom devoir</th>
        <th>duree devoir</th>
        <th>date de creation</th>
        <th>contenu</th>
        <th>category</th>
        <th>Modifier</th>
        <th>Supprimer</th>
        <th>Questions</th>
</tr>

{% for i in cc %}
<tr>
        <td>{{i.namedevoir}}</td>
        <td>{{i.dureedevoir}}</td>
        <td>{{i.datecreation}}</td>
        <td>{{i.contenu}}</td>
        <td>{{i.category}}</td>
        <td> <a href="{{path('devoirUpdate',{id:i.iddevoir})}}"> modif  </td>
        <td> <a href="{{path('devoirremove',{id:i.iddevoir})}}"> Remove  </td>
        <td> <a href="{{path('app_question',{id:i.iddevoir})}}"> questions</td>
</tr>

{% endfor %}
</table>

<button class="middle"   type="button"><a href="{{path('ajoutdevoir')}}"> Ajoute </button>

        <div class="bottom" id="div_histogram"></div>

        <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
        <script type="text/javascript">
                {{ gc_language('fr') }}

                {{ gc_draw(pieChart, 'div_histogram') }}
        </script>

</div>
{% endblock %}

